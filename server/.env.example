# SR OPTICALS Backend Environment Variables
PORT=5000

# Optional: Firebase Admin SDK Service Account credentials
# (If omitted, the server operates seamlessly with the built-in storage service)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=
